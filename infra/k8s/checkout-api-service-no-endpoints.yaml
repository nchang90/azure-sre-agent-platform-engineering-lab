apiVersion: v1
kind: Service
metadata:
  name: checkout-api
  labels:
    app: checkout-api
spec:
  type: ClusterIP
  selector:
    app: checkout-api-shadow
  ports:
    - name: http
      port: 80
      targetPort: 80
